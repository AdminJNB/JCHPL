import React from 'react';
import { Box, Typography } from '@mui/material';

const Settings = () => {
  return (
    <Box>
      <Typography variant="h5" fontWeight="bold" gutterBottom>
        Settings
      </Typography>
      {/* Empty for now — items will be added later */}
    </Box>
  );
};

export default Settings;
